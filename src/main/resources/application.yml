spring:
  mvc:
    static-path-pattern: /static/**
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/websynthsimpletext
    username: root
    password: toor
    driver-class-name: com.mysql.cj.jdbc.Driver